package com.example.study_app;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class QuizTakeActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_quiz_take);

        // TODO: Phát triển QuizTakeActivity trong tương lai
        // Activity này sẽ cho phép người dùng làm bài quiz
        // Bao gồm: hiển thị câu hỏi, đếm thời gian, chấm điểm, hiển thị kết quả
    }
}
